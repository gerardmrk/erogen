syntax = "proto3";

message RendererRequest {
    string url = 1;
    string lang = 2;
    bool authenticated = 3;
}

message RendererResponse {
    int32 status_code = 1;
    RenderError error = 2;
    string redirect_to = 3;
    string lang = 4;
    bytes metas = 5;
    bytes app = 6;
    bytes links = 7;
    bytes styles = 8;
    bytes scripts = 9;
    bytes initial_state = 10;
    string ttr = 11; // TODO: revert back to uint64 when protobufjs supports Node v10's bigint type

    message RenderError {
        string message = 1;
        string stack_trace = 2;
    }
}
