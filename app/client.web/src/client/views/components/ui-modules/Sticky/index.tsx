/**
 * Sticky content stays fixed to the browser viewport
 * while another column of content is visible on the page.
 *
 * --> https://react.semantic-ui.com/modules/sticky/
 */
import "semantic-ui-less/definitions/modules/sticky.less";
import { Sticky } from "semantic-ui-react";
export default Sticky;
