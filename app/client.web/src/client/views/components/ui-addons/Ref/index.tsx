/**
 * This component exposes a prop that supports functional and createRef() API
 * and returns the DOM node of both functional and class component children.
 *
 * --> https://react.semantic-ui.com/addons/ref/
 */
import { Ref } from "semantic-ui-react";
export default Ref;
